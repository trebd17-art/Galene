function showScreen(screenId){document.querySelectorAll(".screen").forEach(screen=>{screen.classList.remove("active")});document.getElementById(screenId).classList.add("active")}
function getCheckedValues(){return Array.from(document.querySelectorAll('.multi input[type="checkbox"]:checked')).map(item=>item.value)}
function buildPlan(){const improvements=getCheckedValues();const challenge=document.getElementById("challenge").value;const time=document.getElementById("timeCommitment").value;const hope=document.getElementById("hope").value;const userType=document.getElementById("userType").value;const improvementText=improvements.length?improvements.join(", "):"your steady mind";document.getElementById("planText").innerHTML=`You want to improve <strong>${improvementText}</strong> and feel more <strong>${hope}</strong>.<br><br>Your biggest challenge right now is <strong>${challenge}</strong>.<br><br>Start with a <strong>${time}</strong> daily routine designed for a <strong>${userType}</strong>.`;showScreen("dashboard")}